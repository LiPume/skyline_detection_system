# Runtime videos are versioned with Git LFS so the repository stays usable while
# a fresh clone can still obtain every demo with `git lfs pull`.
frontend/public/demo/**/*.mp4 filter=lfs diff=lfs merge=lfs -text
demo1/*.mp4 filter=lfs diff=lfs merge=lfs -text
